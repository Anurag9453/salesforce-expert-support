import { LoadingState } from "@/components/ui";

/** Sign-in and registration. Small pages, but a blank frame still looks broken. */
export default function Loading() {
  return <LoadingState label="Loading…" />;
}
