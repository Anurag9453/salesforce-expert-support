{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "//": "allow is broad so routine dev work does not prompt. Bash is unrestricted (tool-wide, not a prefix pattern) per explicit user request on 2026-08-05 — the ask list that used to gate destructive git/db/infra commands has been emptied. `deny` still takes precedence over `allow` for the .env read protections below.",
    "allow": [
      "Bash",
      "Read(*)",
      "Write(*)",
      "Edit(*)",
      "Glob(*)",
      "Grep(*)",
      "TodoWrite(*)",
      "WebFetch(*)",
      "WebSearch(*)"
    ],
    "ask": [],
    "//deny": ".env.example is committed and holds only placeholders, so it stays readable; every real .env is denied.",
    "deny": [
      "Read(./.env)",
      "Read(./.env.local)",
      "Read(./.env.development)",
      "Read(./.env.production)",
      "Read(./.env.test)",
      "Read(./apps/**/.env)",
      "Read(./apps/**/.env.local)",
      "Bash(cat .env)",
      "Bash(cat ./.env)"
    ]
  }
}
