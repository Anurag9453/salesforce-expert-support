# Keep the dependency graph honest — no implicit hoisting.
# packages/domain must fail loudly if it reaches for something it never declared.
strict-peer-dependencies=false
auto-install-peers=true
resolution-mode=highest
