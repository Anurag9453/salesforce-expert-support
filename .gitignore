node_modules/
.pnpm-store/

# builds
dist/
.next/
out/
*.tsbuildinfo

# turbo
.turbo/

# env — never commit
.env
.env.*
# Templates only. Both are placeholders — the `.env.*` rule above is what keeps
# every filled-in variant out, including .env.supabase and .env.production.local.
!.env.example
!.env.vercel.example

# local embedded postgres data dir + binaries
.postgres/
.pgdata/

# test / coverage
coverage/
test-results/
playwright-report/

# editor / os
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# sentry
.sentryclirc
.storage/

# Salesforce CLI local state. `.sf` and `.sfdx` hold org auth — an access token
# in git is a token published to everyone with repo access.
.sf/
.sfdx/
salesforce/.sf/
salesforce/.sfdx/
